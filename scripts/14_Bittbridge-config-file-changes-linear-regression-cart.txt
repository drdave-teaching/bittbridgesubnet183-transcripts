Okay, cool — now my favorite part: how to work and experiment with advanced machine-learning models and deploy them on the miner side. First, please read these disclaimers. It's not a lot of text, but it's important for understanding the next steps, and it will save you a lot of time.

First, we need to make some changes in our configuration file. For example, I want to turn on the time features. Inside this configuration file there are comments and notes explaining what each setting is and how to work with it. Some of them are booleans — true or false — and some take a value. So please read what's going on here. For now, I just want to turn on the time features and leave the rest off — as you can see, they're set to "false". I don't want to change the training/validation split, and I really don't want to change the model configurations or hyperparameters. Please don't edit that — leave it default.

So what's going on here? These are features you can turn on and off, applied to the training and test datasets. Then there's a block with the models' hyperparameters. For example, for the decision-tree models you can increase or decrease the max depth, or add or remove the leaf size; for LSTM and RNN models you can tweak the parameters too. But for this example, I just want to turn on the time features and see how my models perform. Let me make the edits, and in my text editor I'll note my experiment: "turn on time features." Let me save the file — Ctrl+O, Enter, Ctrl+X. The configuration file is saved.

Now let me go to my tmux session: "tmux attach" to the "miner" terminal. I'm inside it. Let's clear the terminal output, and run the miner with the same command as before. It asks whether I want to use the baseline moving-average model, but since we already used it, I don't — no. Then it asks whether I want to update the training dataset; my last update was a couple of days ago, so I'll update it. For your information: the first time you run all of this, it will take two or three minutes to fetch the training-dataset information, but it happens automatically — you just need to wait. Let me pause here. Yes, I want to update the training data, and the first model I want to run is linear regression. It'll fetch the latest data, so I'll come back when it's done.

It's done. Let's enter navigation mode in tmux — Ctrl+B, then that key — and scroll up. I can see my dataset got fetched: here's the shape, here's how long it took, and here are the metrics of the linear-regression model on the training and validation datasets. I can also see a visual of how the model performed, actual versus predicted — the first graph is on the training dataset, the second on the validation dataset. It asks whether I really want to deploy this model. Let me exit navigation mode by pressing "Q". I don't want to deploy this one, because the performance is really bad, so I press "no". I want to train the CART model with the same configuration, so I press "2".

This should be pretty fast. Let's enter navigation mode and check the metrics. From the visual, it has better metrics — our R-squared is better, and the R-squared on the validation set is better than a random guess, so it's definitely better than linear regression. I'll note this as experiment number one: "linear regression / metrics / CART," and copy it from here.

What's next? Let's say I want to deploy this model, so I press "yes". First I quit navigation mode with "Q", then press "yes". Now, on the fly, everything is automatic — your miner will deploy. After a maximum of five minutes you'll see a success log that your CART model got deployed, and after five to ten minutes you'll see success logs that the miner is really using your model to make predictions. Let me pause here and continue in the next video.
