Okay, cool. As we can see from the logs, I deployed my miner at 11:13 and it deployed with the CART model. At 11:15 it made a prediction for the next five minutes using the CART model.

So, if I want to experiment with the configuration features and hyperparameters, I don't necessarily need to stop tmux first. I can keep my miner working and just detach from the session — Ctrl+B, then "D" — and run experiments here. Let me make some changes in the config file: "nano model_params.yaml". For example, I want to include a humidity feature, so I just add it here. Now my model will use, in the training, validation, and test datasets, not only the time features but also the temperature feature and the relative-humidity feature. I've made those changes. I also want to increase the depth of the CART model — instead of six, I'll set it to seven. Ctrl+O to save, Enter, Ctrl+X.

If I want to experiment and see how my model performs without necessarily stopping the miner, I can run this command here to run the model. I don't want to run the baseline model, and I don't want to update my training datasets since they're recent — ten minutes ago — so no. I want to see how my CART model performs with these new changes. Just for reference, previously these were my metrics. Let's check. With only this small change — let me track it as experiment two: "add temp + humidity, and increase CART depth to seven." We can see that with only this small change, my metrics are much, much better. Even if the validation-set R-squared isn't dramatically bigger than the previous experiment, there's definitely an improvement, and the model improved much more on the training set. I'll track these metrics here.

Now I can restart my miner — but I can't do it here, I need tmux. So when it asks whether I want to deploy this model, I choose "no" and exit the miner. I go to tmux to restart it: "tmux attach" to the "miner" terminal. Here I just rerun the command, and it will automatically — I don't want to use the baseline, don't update data, no — I want to run CART. It will retrain one more time, but with the new features and the new hyperparameters. If I want to deploy it, I just press "Y", and it deploys and the miner runs.

I'll pause here and continue in the next video.
