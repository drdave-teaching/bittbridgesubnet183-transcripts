Okay, so what happens in part two? We just need to copy-paste all the commands from here to update all the packages on this OS — like when macOS or Windows tells you that you need to install some updates. Let's copy-paste all these commands; it'll take some time, but not long. I'll pause when it's done.

Okay, the next command. We can type "clear" to clear the terminal. Then let's create a directory with "mkdir" and give it a name. We can run "ls" — the list command — and see that the folder was created, just like creating a folder on your Mac or in Windows. Then type "cd" followed by the folder name to go inside it. We can see we're inside the folder now.

Next we need to git-clone our repository: "git clone". Done. What happened here? We cloned the entire repository. If we run "ls" again, we can see there's a BittBridge folder. Let's go into it. As you can see from the path, we're inside the BittBridge folder, and if we run "ls" one more time, we can see the entire structure of our project — the directories: bittbridge, contribute, docs, miner, model, neurons, scripts, and everything else. This is cool, right?

Our next step is to create the virtual environment. We're inside our BittBridge folder; let's create it. When you see "(venv)" like this, it means you've created and activated your virtual environment. Now let's install all the necessary dependencies with this command. It'll take some time, so I'll take a break and come back when it's done.

It's done — we can see everything installed. We can verify with this command. Let's check: it's a live version, the latest version. Let's check Bittensor — yep, "bittensor 4.x", great news. I'll pause here, and in the next video we'll create the wallets and fill them up with tokens.
