Hey everyone — long time no see. We've been working a lot on development and testing to make your life and your education more entertaining, and to make some things easier for you. So let's start. We stopped last time, I think, on deploying the moving-average miner, so I assume you have a running moving-average miner. Next we'll cover update and restart, because a lot of changes were made in the remote repository's main branch, and we'll need to fetch and apply them. But first, I want to show you some Linux basics, so it's easier for you to navigate and understand what you're doing.

Let's open my terminal window. First, I want to clear all this irrelevant output — I just press Enter and run the "clear" command. Now the terminal is clear. Let's start with the first command: "pwd", print working directory. It shows the path of where you are right now. Next, the "ls" command just prints the list of all the folders inside your current working directory. Then "cd" means change directory — let's go into "advanced-ml-testing". Now I'm inside this folder, and if I run "ls" again, I can see the BittBridge folder. Let's dive deeper into it with "ls" one more time — I can see all the files in the BittBridge folder.

What is "cd .."? Change directory, double-dot, goes up one level. So now if I run "ls", I can see I'm one level above. Let's go back into BittBridge. Now I'm inside the BittBridge folder, and if I run "pwd" I can see the path — where am I right now. Let's clear it up.

What's next? "nano". This command opens the basic Linux text editor. Let's edit the configuration file, "model_params.yaml" — this is the main file you'll work with. I can scroll up and down and make all my changes here. For example, let's change this "false". To save, press Ctrl+O; it asks whether we want to change the file name — I don't want to change anything, so I just press Enter — then press Ctrl+X to close it.

How do I do this? I just press the up or down arrow to scroll through the previous commands I've run in this terminal. Also, with Ctrl+R I can search for a previously executed command. For example, I want to activate the virtual environment, so I type "source" and it finds the "source ... venv" command I ran some time ago. I press Enter and it runs.

Let me show you one last thing: how to scroll inside a tmux session. I assume you have your running miner in a "tmux attach" terminal, and as you can see, my miner is running — but if I try to scroll, it doesn't let me. To do it, press Ctrl+B and then this key — sorry, I don't remember its name. When you see the yellow numbers in the top-right corner, it means you've entered navigation mode, and now I can use my mouse or arrows to scroll up and down. To quit navigation mode, just press "Q", and the tmux session keeps running without me.

Okay, I'll stop here. In the next recording I'll explain how to update your repository to the latest code base and fetch all the most recent changes. Thank you.
