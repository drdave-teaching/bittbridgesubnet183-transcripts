Let's go to the next step: the Google Cloud Platform virtual-machine setup. You can follow these guides step by step — there are screenshots showing how to do it — but let me do it live.

When you start, you'll land on this welcome page. You can just type "virtual machine" here and go to VM instances. First, let's activate the free trial. Done.

First things first, we need to create a firewall rule. What does the firewall rule do? It opens your virtual machine to the rest of the network. The whole point of decentralization is that miners and validators are separate machines, run by separate people in different parts of the world, and they need to be able to talk to each other. That's why we create a firewall rule that opens your virtual machine to the rest of the world.

Just type "firewall rules" here and click "Create firewall rule." For the name, I use "allow-tcp-8091" — 8091 is the port number. Set the direction to ingress, the action on match to allow, and the targets to "all instances in the network." Then under protocols and ports, choose specified protocols and ports, check TCP, and enter port 8091. Then click Create. I already have this rule, so I've set it up here — as you can see, all the settings are exactly as I showed, and it will now apply to all the virtual machines in your Google Cloud Platform account.

Let's go back to VM instances and click "Create instance." You can leave the name as the default. The next step is to change the boot disk — we need to change the OS this virtual machine will use. The OS is like your macOS or Windows, but for virtual machines, most developers use Linux, since it's lightweight, fast, and a lot of projects run on it without any problems. Click "Change," and following the guide, we'll use Ubuntu. Copy-paste the exact version — I'm looking for version 22.04 LTS, x86/64 — yes, that's correct. Change the disk size to 25 gigabytes and click Select. Our next step is to create the virtual machine itself; the rest of the settings stay default. Click Create.

It'll take some time to deploy — as you can see, it's pending, it's creating. Please don't forget to switch it off after the course, since it costs around twenty-five or twenty-six dollars per month. You have three hundred dollars of free trial, but it will run out sooner than you think. I'll pause here and continue when it's done.

Okay, cool, it's done. Click "SSH" here and it will open a new window. What's happening here is that we're connecting to our virtual machine. Now we have a real computer somewhere on Google's servers where we can do anything that comes to mind. This is the final step here, and in the next video we'll go to part two.
