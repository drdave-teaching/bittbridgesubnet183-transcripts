Before I leave you to experiment on your own, I just want to cover a few last things. First, let me detach from tmux — Ctrl+B, then "D". I've detached from the session.

Suppose I want to see how my last CART model's predictions actually performed. I need to go to the artifacts folder. As you can see, I'm now in the artifacts directory. I know I ran the CART model about five minutes ago — so, April 26th, this is the most recent run. The time here is in UTC, so it's a little off, but I think this is the most recent model. Let's go inside the folder. Here you can see the five main files, and I can download all of them. There's the model — "model_cart.joblib" — which is the dump of the model weights; you can download it and run it locally. There's "metrics.json", which stores the metrics of this particular run; "manifest.json"; and "config_snapshot.yaml", the config you used. The manifest holds the data shape and the timestamp of the training data used.

For example, I just want to download the actual-versus-predicted file. I need to know its path — "realpath actual_vs_predicted". I have the path, so I copy it, click "download file" here, paste it, and... sometimes it doesn't work on the first attempt. Let me refresh this page with Ctrl+R. Okay, authorize. Now: "ls", go to the BittBridge folder, move to the artifacts folder, and I want the actual-versus-predicted for this run. One more time: "realpath" the predicted file, go to that path, download file, paste it, download. We can see it's downloading, and it will appear in your Downloads folder. That's the first thing I wanted to show you.

The second thing: if something gets stuck, just press Ctrl+C or close your terminal window and open a new one from your Google Cloud Platform window. And you can track everything on our website — here's a description of what's going on, and here's our miner leaderboard. For example, on the latest prediction, the miner with UID 2 made the best prediction. Okay, let me show you in the next video how to track your performance. I'll pause here.
