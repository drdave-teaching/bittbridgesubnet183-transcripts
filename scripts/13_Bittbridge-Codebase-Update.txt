Okay, let's go to the next step. First, we need to stop the running miner so we can fetch all the changes. I'll open my tmux session — let me show you how one more time. Just run "tmux attach" to the "miner" terminal. Now I'm inside the tmux terminal, and I just need to stop the miner by pressing Ctrl+C, which stops execution of the Python command. We can see it's stopping the miner. Okay, the miner is stopped. Now I detach from the tmux session with Ctrl+B then "D". I've detached from the session — you can see the logs here.

Next I need to pull the latest code. How do I do it? I'm inside the BittBridge folder, so I run "git pull", and it pulls all the recent changes that were made to the main branch of the remote repository. Sometimes you'll see an error saying something like "please commit your changes or stash them before you merge." If that appears on your end, just run "git reset --hard" and then "git pull".

What's next? I assume you've done the "git pull" and fetched all the changes. Now you need to activate the virtual environment, so let me rerun the "source" command. I can see the "(venv)" label on the left, which means my virtual environment is activated. Now I need to install all the new requirements with "pip install -r requirements.txt". As you can see, all the requirements were already installed on my end. I'll run "clear".

So that's mostly it. We've updated our virtual machine's code base and aligned it with the most recent version from the remote repository. I'll pause here, and next I'll show you how to work with and deploy advanced machine-learning models on the miner side. Thank you.
