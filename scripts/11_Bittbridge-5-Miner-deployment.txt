So the final step is to actually run the miner. I can just press "next" here, and we're on this page. We'll do it using tmux. What is tmux? It's a special application on Linux that lets you run your scripts in the background. For example, if I run a Python script normally and then close this window, it stops — the window closes and the script stops with it. That's why we use tmux: it lets the script keep running in the background, so even if we close the window, it continues to work as long as the virtual machine is deployed on the Google Cloud Platform.

I'll copy this command and paste it here. Don't forget you need to activate your virtual environment first with "source". We can also update the moving-average model if we want. To do that, run "ls", go to "cd neurons", "ls", and then "nano miner.py". We can update the number here — let's change it from 12 to 24, so we fetch moving-average data for the last two hours, since each time frame is five minutes. Press Ctrl+X to save, "Y" for yes, and Enter. We've updated it.

Our next step is to run the command. We need to run this script from the BittBridge directory, so we go back to the main folder. To go up one level, use "cd .." — as you can see, before we were in the bittbridge/neurons directory, and now we're in the bittbridge directory itself. I can use the up arrow to find the command I pasted earlier, and press Enter. Now, magic — we can see logs that the miner is trying to deploy. We see an Axon (the server) got deployed on this IP and address, and "miner running," which is a good sign. The miner got deployed onto subnet number 183, and here are the miner's credentials. Good news.

I'll pause here, but in five to ten minutes you'll see logs like this — success logs showing the miner making a prediction for the validator's request. Okay, I'll pause here.

I think that's it. Now you can bravely close this window, and it will keep working in the background without your interaction. If you want to detach from the tmux session, press Ctrl+B, release, then "D". Now I can do whatever I want here. Using "tmux ls" — the list command — we can see there's one miner window currently running, and we can reattach to it with "tmux attach". In five or ten minutes you'll see the success logs.

And there it is — the success logs. Don't be surprised by this error; it's fine, just some logs. We can see "success": the miner was asked by a validator to make a prediction, and using the moving average of the most recent energy data, it predicted that for the next five minutes the energy demand is going to be this value. How cool is that? Now you can tell everyone that you've successfully deployed a prediction model onto a decentralized-AI network.

So in this small assignment you actually used the Google Cloud Platform, the decentralized-AI network, the blockchain itself, a predictive model, and Linux. I think I forgot something — there's no way I didn't — but you touched a lot of things, and that's very cool. Thanks, everybody, for watching, and enjoy the rest of your day.
